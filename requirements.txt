# Compatibility entry point; pyproject.toml is the dependency source of truth.
# Recommended reproducible installation: uv sync --locked
# Unlocked pip fallback (Python 3.12+): pip install -r requirements.txt
-e .
